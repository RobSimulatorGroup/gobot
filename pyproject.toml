[build-system]
requires = [
    "mujoco==3.10.0; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "packaging>=23.2",
    "scikit-build-core>=1.0.3",
]
backend-path = ["."]
build-backend = "gobot_build_backend"

[project]
name = "gobot"
version = "0.1.18"
description = "Python bindings for the Gobot robotics scene, simulation, and rendering engine."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [
    { name = "RobSimulatorGroup" },
]
dependencies = [
    "imageio>=2.34",
    "imageio-ffmpeg>=0.5",
    "mujoco==3.10.0; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "mujoco-warp==3.10.0.2; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "newton[onnx,sim]==1.4.0; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "numpy>=2,<3",
    "nvidia-cuda-runtime-cu12>=12.8,<13; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "onnx>=1.16",
    "onnxruntime>=1.19",
    "rsl-rl-lib==5.2.0",
    "scipy>=1.11",
    "tensorboard",
    "tensordict",
    "torch>=2.7; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "trimesh>=4.6.8; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "warp-lang==1.15.0; sys_platform == 'linux' and platform_machine == 'x86_64'",
]

[project.urls]
Homepage = "https://github.com/RobSimulatorGroup/gobot"
Repository = "https://github.com/RobSimulatorGroup/gobot"
Issues = "https://github.com/RobSimulatorGroup/gobot/issues"

[project.scripts]
gobot_editor = "gobot_cli.editor:editor"

[dependency-groups]
dev = [
    "cmake>=3.27",
    "ninja>=1.11",
    "packaging>=23.2",
    "scikit-build-core>=1.0.3",
]

[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true

[tool.uv.sources]
torch = [
    { index = "pytorch-cu128", marker = "sys_platform == 'linux'" },
]

[tool.scikit-build]
cmake.version = ">=3.27"
cmake.build-type = "Release"
build-dir = "build/{wheel_tag}"
wheel.packages = ["python/gobot", "python/gobot_cli"]

[tool.scikit-build.install]
components = ["python"]

[tool.scikit-build.cmake.define]
GOB_BUILD_TESTS = "OFF"
GOB_BUILD_ASSIMP = "ON"
GOB_BUILD_MUJOCO = "ON"
GOB_BUILD_EGL = "ON"
GOB_BUILD_LUISA_RENDERER = "ON"
GOB_BUILD_GSPLAT_INFERENCE = "ON"
GOB_BUILD_OPENUSD = "ON"
GOB_BUNDLE_OPENUSD_RUNTIME = "ON"
GOB_FETCH_MUJOCO = "OFF"
GOB_FORCE_FETCH_MUJOCO = "OFF"
GOB_MUJOCO_GIT_TAG = "3.10.0"
