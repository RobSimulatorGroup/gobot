# Gobot resolves libuipc's pinned dependencies before adding its source tree.
# This intentionally empty file only satisfies libuipc's unconditional
# CMAKE_TOOLCHAIN_FILE check; it does not install packages or change compilers.
